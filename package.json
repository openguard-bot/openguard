{
  "name": "openguard",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "website",
    "dashboard/frontend"
  ],
  "repository": "https://github.com/discordaimod/openguard.git",
  "author": "Slipstream <me@slipstreamm.dev>",
  "license": "MIT",
  "scripts": {
    "build": "yarn workspaces foreach -pt --exclude openguard -A run build",
    "test": "yarn workspaces foreach -pt --exclude openguard -A run test",
    "lint": "yarn workspaces foreach -pt --exclude openguard -A run lint"
  },
  "devDependencies": {
    "eslint-formatter-github": "^1.1.4"
  }
}
